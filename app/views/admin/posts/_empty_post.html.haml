%h2
  New Empty Post

= form_with(model: Post.new, class: "row g-3") do |form|
  %div{ class: "row mb-3 " }
    .field
      = form.label :title, class: "col-sm-2 col-form-label"
      = form.text_field :title, class: "form-control"
  .actions
    = form.submit class: "btn btn-primary mb-3"
    